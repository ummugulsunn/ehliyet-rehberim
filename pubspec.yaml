name: ehliyet_rehberim
description: "Ehliyet Rehberim - Türkiye'nin en kapsamlı ehliyet sınavı hazırlık uygulaması. 20 deneme sınavı, trafik işaretleri, konu anlatımları ve detaylı analizler ile ehliyet sınavınıza hazırlanın."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.4+8

environment:
  sdk: ^3.8.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  
  # Riverpod for state management
  flutter_riverpod: ^2.6.1

  
  # Google Fonts for Inter font family
  google_fonts: ^6.2.1
  flutter_markdown: ^0.7.2
  
  # Firebase Authentication
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.3
  cloud_firestore: ^5.6.1
  
  # Google Sign-In
  google_sign_in: ^6.2.1
  
  # Sign in with Apple
  sign_in_with_apple: ^6.1.3
  
  # Local storage for user progress
  shared_preferences: ^2.2.2
  path_provider: ^2.1.5
  intl: ^0.20.2
  
  # Confetti animations for quiz celebrations
  confetti: ^0.7.0
  
  # Charts for statistics visualization
  fl_chart: ^0.69.0
  
  # Local notifications for reminders
  # flutter_local_notifications: ^18.0.0
  # timezone: ^0.9.4

  in_app_review: ^2.0.9
  url_launcher: ^6.3.0
  package_info_plus: ^8.0.2
  smooth_page_indicator: ^2.0.1
  
  # WebView for video embeds (Vimeo)

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  
  # App icon generator
  flutter_launcher_icons: ^0.13.1
  flutter_native_splash: ^2.4.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/data/exams.json
    - assets/data/traffic_signs.json
    - assets/images/deneme_sinavi_1_soru_39/
    - assets/data/study_guides.json
    - assets/images/
    - assets/images/guides/
    - assets/images/traffic_signs/
    - assets/images/downloaded/deneme_sinavi_21/
    - assets/images/downloaded/deneme_sinavi_22/
    - assets/images/downloaded/deneme_sinavi_23/
    - assets/images/downloaded/deneme_sinavi_24/
    - assets/images/downloaded/deneme_sinavi_25/
    - assets/images/downloaded/deneme_sinavi_26/
    - assets/images/downloaded/deneme_sinavi_27/
    - assets/images/downloaded/deneme_sinavi_28/
    - assets/images/downloaded/deneme_sinavi_29/
    - assets/images/downloaded/deneme_sinavi_30/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # Fonts are handled by Google Fonts package
  # No local font assets needed
  
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# Flutter Launcher Icons configuration
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/app_logo.png"
  min_sdk_android: 21
  # Android specific settings for better icon fit
  android_gravity: center
  # iOS specific settings
  ios_content_mode: center
  remove_alpha_ios: true
  # Web settings
  web:
    generate: true
    image_path: "assets/images/app_logo.png"
    background_color: "#FFFFFF"
    theme_color: "#4F46E5"
  # Windows settings
  windows:
    generate: true
    image_path: "assets/images/app_logo.png"
    icon_size: 48
  # macOS settings
  macos:
    generate: true
    image_path: "assets/images/app_logo.png"

# Flutter Native Splash configuration
flutter_native_splash:
  color: "#FFFFFF"
  image: "assets/images/app_logo.png"
  android_12:
    image: "assets/images/app_logo.png"
    color: "#FFFFFF"
  web: false
